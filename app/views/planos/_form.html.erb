<%= form_with(model: plano) do |form| %>
  <% if plano.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(plano.errors.count, "error") %> prohibited this plano from being saved:</h2>

      <ul>
        <% plano.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.select(:status, Plano.statuses.keys.map {|status| [status.titleize,status]}, :class => 'form-control' )%>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
 <style>
   body { margin: 2%; }
</style>